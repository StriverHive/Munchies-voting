# Unit tests (reference)

Executable **server** unit tests live in:

`server/tests/unit/`

Run: `npm test --prefix server`

(Client “unit-style” checks for small pure helpers live next to UI tests under `client/src/tests/ui/`.)
