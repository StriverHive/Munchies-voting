# Visual regression (optional)

Not enabled by default. To add visual coverage:

- **Percy** + Storybook or Playwright snapshots
- **Chromatic** for component libraries
- **Playwright** `toHaveScreenshot()` against stable staging URLs

Keep baselines out of the main branch until the design system stabilizes.
