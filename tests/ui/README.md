# UI / component tests (reference)

Executable **React** tests live in:

`client/src/tests/ui/`

Run: `npm run test:ci --prefix client`

Global test setup: `client/src/setupTests.js` (mocks for `axios`, `lottie-react`, and common browser APIs in jsdom).
